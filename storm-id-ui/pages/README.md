This directory exists to satisfy Next.js's requirement that `pages` and `app` directories be under the same folder when using App Router.

All actual page components live in `src/pages/` following FSD architecture.
